# https://github.com/AlexxIT/go2rtc#module-api for other configuration examples
# Example
streams:
  tardis:
    - exec:ffmpeg -re -i udp://225.6.100.90:21000 -f rtsp -rtsp_transport tcp rtsp://localhost:8554/live.stream
log:
  level: info  # default level
  api: debug
  rtsp: debug
  streams: debug
  webrtc: debug